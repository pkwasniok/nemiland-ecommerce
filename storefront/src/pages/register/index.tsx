import { PageLayout } from "@/features/layout";

const RegisterPage = () => {
  return (
    <PageLayout
      title="Rejestracja"
      backlinkHref="/login"
      showTitle
    ></PageLayout>
  );
};

export default RegisterPage;
