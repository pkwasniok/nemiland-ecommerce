import { PageLayout } from "@/features/layout";

const LoginPage = () => {
  return (
    <PageLayout title="Logowanie" showTitle>
      Test
    </PageLayout>
  );
};

export default LoginPage;
