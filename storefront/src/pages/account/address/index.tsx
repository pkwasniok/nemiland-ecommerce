import { PageLayout } from "@/features/layout";

const AddressesPage = () => {
  return (
    <PageLayout
      title="Moje adresy"
      backlinkHref="/account"
      showTitle
    ></PageLayout>
  );
};

export default AddressesPage;
